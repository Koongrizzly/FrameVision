# GPU requirements for describer/LLM/VL stack (no torch here; installed earlier by Option 4)
transformers==4.44.2
accelerate==0.33.0
safetensors==0.4.4
sentencepiece==0.2.0
einops==0.8.0
# Optional / if used elsewhere
bitsandbytes==0.43.3 ; platform_system != "Windows"
