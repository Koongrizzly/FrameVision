# Standalone FrameVision-style LTX 2.3 runtime requirements.
# Torch is intentionally NOT listed here. Installer installs CUDA Torch first:
#   torch==2.8.0 torchvision==0.23.0 torchaudio==2.8.0 --index-url https://download.pytorch.org/whl/cu128
# Never add unpinned torch here, because pip may replace CUDA Torch with CPU Torch.

pip>=24.0
setuptools>=70
wheel
uv_build>=0.9.8,<0.10.0

transformers>=4.52,<4.58
accelerate>=1.0
safetensors==0.7.0
scipy>=1.14
numpy>=1.26,<2.0
einops
av
tqdm
pillow>=10
diffusers>=0.35
huggingface_hub>=0.35
sentencepiece>=0.2
tokenizers>=0.21
protobuf
imageio
imageio-ffmpeg
OpenImageIO
soundfile
omegaconf
packaging
requests
PySide6>=6.7
