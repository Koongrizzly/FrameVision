--- helpers/framevision_app.py (original)
+++ helpers/framevision_app.py (with GAB wiring)
@@ -13,6 +13,7 @@
 from helpers.tools_tab import CollapsibleSection as ToolsCollapsibleSection
 from pathlib import Path
 
+from helpers.quick_action_button import QuickActionDriver
 # --- Block creation of any directory containing "framelab" (legacy) ---------
 try:
     import os as _os
@@ -1347,6 +1348,13 @@
         # right: hud + tabs
         self.hud = HUD(self)
         self.tabs = QTabWidget(self)
+        # Context-aware Global Action Button driver
+        try:
+            self._gab = QuickActionDriver(self, self.tabs, self.video.btn_upscale)
+            self._gab.install()
+        except Exception as _e:
+            print('[GAB] init skipped:', _e)
+
         self.edit = InterpPane(self, {
             'ROOT': ROOT,
             'BASE': BASE,
@@ -1544,4 +1552,4 @@
     sys.exit(app.exec())
 
 if __name__ == "__main__":
-    main()
+    main()